# Copy to `.env` and fill in values. `.env` is gitignored.

# From https://firecrawl.dev — required for Live Target Analyzer
FIRECRAWL_API_KEY=fc-c3599115fea34e899325db7b932ff3f7

# Optional: only if you self-host Firecrawl
# FIRECRAWL_API_URL=https://api.firecrawl.dev
